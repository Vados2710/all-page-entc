var yknotCamp='<?xml version="1.0" encoding="UTF-8"?><ynot><replace select="#offerIndex"><![CDATA[ <form name="opt_in_form" id="opt_in_form">  <input type="hidden" id="step" name="step" value="1">  <input type="hidden" id="AFID" name="AFID" value="2077-2044"/><input type="hidden" id="SID" name="SID" value="200833"/><input type="hidden" id="C1" name="C1" value="32797006"/><input type="hidden" id="AID" name="AID" value=""/><input type="hidden" id="BID" name="BID" value=""/><input type="hidden" id="bdct" name="bdct" value="1"/><input type="hidden" id="country_ip" name="country_ip" value=""/><input type="hidden" name="product_brand_name" id="product_brand_name" value="Zephrofel Male Enhancement"><input type="hidden" name="iorder_total" id="iorder_total" value="6.82"><input type="hidden" name="terms_required" id="terms_required" value="0"><input type="hidden" name="offer_id" id="offer_id" value="43882"><input type="hidden" name="ipAddress" id="ipAddress" value="89.169.23.106"><input type="hidden" name="friend_sent" id="friend_sent" value="0">  <table border="0" cellpadding=0 cellspacing=0 id="step1">    <tbody id="fields_fname_tr">      <tr>        <td align="right" class="formLeftColWidth"><label for="fields_fname">Όνομα: </label></td>        <td align="left" class="formRightColWidth"><input  type="text" id="fields_fname" name="fields_fname"  class="inputBG" tabindex="1" /></td>      </tr>    </tbody>    <tbody id="fields_lname_tr">      <tr>        <td align="right"><label for="fields_lname">Επίθετο: </label></td>        <td align="left"><input  type="text" id="fields_lname" name="fields_lname"  class="inputBG" tabindex="2" /></td>      </tr>    </tbody>    <tr>      <td align="right"><label for="country">Χώρα: </label></td>      <td align="left"><select size="1" name="country" id="country" class="selectBG" tabindex="3" >                <option value=GR >Ελλάδα</option>           </select></td>    </tr>    <tbody id="fields_address1_tr">      <tr>        <td align="right"><label for="fields_address1">Διεύθυνση: </label></td>        <td align="left"><input  type="text" id="fields_address1" name="fields_address1"  class="inputBG" tabindex="4" /></td>      </tr>    </tbody>    <tbody id="fields_city_tr">      <tr>        <td align="right"><label for="fields_city">Πόλη: </label></td>        <td align="left"><input  type="text" id="fields_city" name="fields_city"  class="inputBG" tabindex="5" /></td>      </tr>    </tbody>    <tr>      <td align="right"><label for="fields_state">Νομός: </label></td>      <td align="left"><div id="state_cus2">          <input type="text" id="fields_state2" name="fields_state2" value="" class="inputBG" tabindex="6" />        </div></td>    </tr>    <tbody id="fields_zip_tr">      <tr>        <td align="right"><label for="fields_zip">Τ.Κ.: </label></td>        <td align="left"><input  type="text" id="fields_zip" name="fields_zip"  class="inputBG" tabindex="7" /></td>      </tr>    </tbody>    <tr>      <td align="right"><label for="fields_phone">Τηλέφωνο: </label></td>      <td align="left"><input type="text" id="fields_phone" name="fields_phone"   class="inputBG" onKeyDown="return onlyNumbers(event);" tabindex="8" /></td>    </tr>    <tbody id="fields_email_tr">      <tr>        <td align="right"><label for="fields_email">ΗΛΕΚΤΡΟΝΙΚΗ ΔΙΕΥΘΥΝΣΗ: </label></td>        <td align="left"><input type="text" id="fields_email" name="fields_email"  class="inputBG" tabindex="9" /></td>      </tr>    </tbody>    <tbody id="fields_email_tr">      <tr>        <td colspan="2" align="center"></td>      </tr>    </tbody>    <tr>      <td colspan="2" align="center"><div id="submit-link"><div onClick="javascript:submitStep1Cart();" tabindex="10" style="cursor:pointer;" class="rush-order" id="rush_order_btn1"> <span id="process">ΦΕΡΤΕ ΜΟΥ ΤΗΝ ΠΑΡΑΓΓΕΛΙΑ ΜΟΥ!</span> </div></div></td>    </tr>  </table></form>]]></replace><replace select="#display_dob"><![CDATA[ <select id="dob_month" name="dob_month"><option value="01">01</option><option value="02">02</option><option value="03">03</option><option value="04">04</option><option value="05">05</option><option value="06">06</option><option value="07">07</option><option value="08">08</option><option value="09">09</option><option value="10">10</option><option value="11">11</option><option value="12">12</option></select><select id="dob_day" name="dob_day"><option value="01">01</option><option value="02">02</option><option value="03">03</option><option value="04">04</option><option value="05">05</option><option value="06">06</option><option value="07">07</option><option value="08">08</option><option value="09">09</option><option value="10">10</option><option value="11">11</option><option value="12">12</option><option value="13">13</option><option value="14">14</option><option value="15">15</option><option value="16">16</option><option value="17">17</option><option value="18">18</option><option value="19">19</option><option value="20">20</option><option value="21">21</option><option value="22">22</option><option value="23">23</option><option value="24">24</option><option value="25">25</option><option value="26">26</option><option value="27">27</option><option value="28">28</option><option value="29">29</option><option value="30">30</option><option value="31">31</option></select><select id="dob_year" name="dob_year"><option value="2006">2006</option><option value="2005">2005</option><option value="2004">2004</option><option value="2003">2003</option><option value="2002">2002</option><option value="2001">2001</option><option value="2000">2000</option><option value="1999">1999</option><option value="1998">1998</option><option value="1997">1997</option><option value="1996">1996</option><option value="1995">1995</option><option value="1994">1994</option><option value="1993">1993</option><option value="1992">1992</option><option value="1991">1991</option><option value="1990">1990</option><option value="1989">1989</option><option value="1988">1988</option><option value="1987">1987</option><option value="1986">1986</option><option value="1985">1985</option><option value="1984">1984</option><option value="1983">1983</option><option value="1982">1982</option><option value="1981">1981</option><option value="1980">1980</option><option value="1979">1979</option><option value="1978">1978</option><option value="1977">1977</option><option value="1976">1976</option><option value="1975">1975</option><option value="1974">1974</option><option value="1973">1973</option><option value="1972">1972</option><option value="1971">1971</option><option value="1970">1970</option><option value="1969">1969</option><option value="1968">1968</option><option value="1967">1967</option><option value="1966">1966</option><option value="1965">1965</option><option value="1964">1964</option><option value="1963">1963</option><option value="1962">1962</option><option value="1961">1961</option><option value="1960">1960</option><option value="1959">1959</option><option value="1958">1958</option><option value="1957">1957</option><option value="1956">1956</option><option value="1955">1955</option><option value="1954">1954</option><option value="1953">1953</option><option value="1952">1952</option><option value="1951">1951</option><option value="1950">1950</option><option value="1949">1949</option><option value="1948">1948</option><option value="1947">1947</option><option value="1946">1946</option><option value="1945">1945</option><option value="1944">1944</option><option value="1943">1943</option><option value="1942">1942</option><option value="1941">1941</option><option value="1940">1940</option><option value="1939">1939</option><option value="1938">1938</option><option value="1937">1937</option><option value="1936">1936</option><option value="1935">1935</option><option value="1934">1934</option><option value="1933">1933</option><option value="1932">1932</option><option value="1931">1931</option><option value="1930">1930</option><option value="1929">1929</option><option value="1928">1928</option><option value="1927">1927</option><option value="1926">1926</option><option value="1925">1925</option><option value="1924">1924</option><option value="1923">1923</option><option value="1922">1922</option><option value="1921">1921</option><option value="1920">1920</option><option value="1919">1919</option></select>]]></replace><replace select="#offerFooter"><![CDATA[ <div align=\"center\"><div class="refer"><p>*Δεν έχει εγκριθεί από οποιαδήποτε διεθνή διοικητικά όργανα. Το παρόν προϊόν δεν προορίζεται για τη διάγνωση, τη θεραπεία ή την πρόληψη οποιασδήποτε πάθησης. Οι πληροφορίες που παρέχονται από τον παρόντα ιστότοπο, το παρόν μήνυμα ηλεκτρονικού ταχυδρομείου ή από την παρούσα εταιρεία δεν υποκαθιστούν τη διαπροσωπική επαφή με έναν επαγγελματία του τομέα της υγείας και δεν θα πρέπει να ερμηνεύονται ως ατομική ιατρική συμβουλή. Εάν παρατηρήσετε αλλαγή στην ιατρική σας κατάσταση, παρακαλώ σταματήστε να χρησιμοποιείτε το προϊόν μας αμέσως και συμβουλευτείτε τον γιατρό σας. Μην το χρησιμοποιείτε εάν η σφραγίδα ασφαλείας έχει παραβιαστεί ή απουσιάζει. Για χρήση μόνο από ενήλικες, να φυλάσσεται μακριά από παιδιά κάτω των 18 ετών.</p><p>Όλοι οι αγοραστές δικαιούνται να λάβουν πλήρη επιστροφή των εξόδων αποστολής και διαχείρισης που κατέβαλαν σήμερα επικοινωνώντας απλά με το τμήμα υποστήριξης ανά πάσα στιγμή εντός 30 ημερών από την αγορά. Για να δικαιούστε πλήρη επιστροφή του συνολικού κόστους των προϊόντων θα πρέπει να επιστραφεί μία σφραγισμένη φιάλη του Zephrofel Male Enhancement συμπεριλαμβανομένου ενός αριθμού RMA. Μπορείτε να λάβετε έναν αριθμό RMA εφόσον επικοινωνήσετε με το τμήμα εξυπηρέτησης πελατών εντός 30 ημερών από τη σημερινή σας αγορά. Εξυπηρέτηση πελατών: +39-0694805066</p><!-- Testimonial Addition --><p>*Τα ονόματα των χρηστών, οι εικόνες και οι τοποθεσίες έχουν τροποποιηθεί για λόγους προστασίας του απορρήτου. Σε ορισμένες περιπτώσεις οι μετρήσεις βάρους έχουν μετατραπεί από το βρετανικό (imperial) στο μετρικό (metric) σύστημα και από το μετρικό στο βρετανικό.</p><p>Δεν φέρουμε ευθύνη για οποιαδήποτε αξιολόγηση, αναφορά, μήνυμα ηλεκτρονικού ταχυδρομείου, διαφήμιση ή ιστολόγιο (blog) που ενδέχεται να σας έχει οδηγήσει στην παρούσα σελίδα.  Εάν θεωρείτε ότι οποιαδήποτε από τις εν λόγω αξιολογήσεις, διαφημίσεις, ιστολόγια (blogs) ή αναφορές τρίτων μερών ενδέχεται να είναι ανακριβής, επικοινωνήστε μαζί μας αμέσως. Δεν καταδικάζουμε ούτε εγκρίνουμε οποιεσδήποτε ανακριβείς πληροφορίες, στατιστικά και/ή ισχυρισμούς τρίτων μερών σχετικά με το προϊόν μας.</p></div><p class=\"site_info\"><span class=\"copyright\"><br /><br />Zephrofel Male Enhancement © 2019 </span><br /><br /></p></div>]]></replace><replace select="#stepHeader_holder"><![CDATA[ <div id="stepHeader" align="right">  <div id="stepThreeWrapper">	  <div id="stepThree" align="center">  Βήμα 3      <h2 align="center">      Επιβεβαίωση      </h2>  </div>  </div><div id="divider"></div><div id="stepTwoWrapper"><div id="stepTwo" align="center">  Βήμα 2      <h2 align="center">      Πληροφορίες<br>πληρωμής      </h2>  </div>  </div><div id="divider"></div> <div id="stepOneWrapper"> <div id="stepOne" align="center">  Βήμα 1      <h2 align="center">      Πληροφορίες<br>αποστολής      </h2>  </div>  </div></div>]]></replace><replace select="#productHeader_holder"><![CDATA[ <div id="productHeader">    <span class="productTitle">    προϊόν    </span>    <span class="priceTitle">   Τιμή    </span></div>]]></replace><replace select="#paymentHeader_holder"><![CDATA[ <div id="paymentHeader"></div>]]></replace><replaceContent select="#offerCartForm"><![CDATA[<form name=opt_in_form8 id=opt_in_form8 method=get><input type="hidden" id="AFID" name="AFID" value="2077-2044"/><input type="hidden" id="SID" name="SID" value="200833"/><input type="hidden" id="C1" name="C1" value="32797006"/><input type="hidden" id="AID" name="AID" value=""/><input type="hidden" id="BID" name="BID" value=""/><input type="hidden" id="bdct" name="bdct" value="1"/><input type="hidden" id="country_ip" name="country_ip" value=""/><input type="hidden" name="product_brand_name" id="product_brand_name" value="Zephrofel Male Enhancement"><input type="hidden" name="iorder_total" id="iorder_total" value="6.82"><input type="hidden" name="terms_required" id="terms_required" value="0"><input type="hidden" name="offer_id" id="offer_id" value="43882"><input type="hidden" name="ipAddress" id="ipAddress" value="89.169.23.106"><input type="hidden" name="friend_sent" id="friend_sent" value="0"></form>]]></replaceContent><eval><![CDATA[ document.title="Zephrofel Male Enhancement";]]></eval></ynot>';
var $_yknot=$_yknot||jQuery;
(function () {
    var version = '1.0';
    var ie_version = getMSIe();
    var is_firefox = navigator.userAgent.toLowerCase().indexOf('firefox') > -1;
    $_yknot.ynot = function (xml) {
        processDoc(xml);
    };

    $_yknot.ynot.debug = 0; // set to true to enable debug logging to
    // window.console.log
    $_yknot.ynot.autodetect = true;
    $_yknot.ynot.defaults = {
        cdataWrap: 'div'
    };

    // add 'replace' and 'replaceContent' plugins (conditionally)

    $_yknot.fn.replace = $_yknot.fn.replace || function (a) {
        this.after($_yknot(a));
        this.remove();
    };
    $_yknot.fn.replaceContent = $_yknot.fn.replaceContent || function (a) {
        return this.empty().append($_yknot(a));
    };

    $_yknot.expr[':'].ynotTag = function (a) {
        return a.ynotTag === 1;
    };

    // allow auto-detection to be enabled/disabled on-demand
    $_yknot.ynot.enableAutoDetection = function (b) {
        $_yknot.ynot.autodetect = b;
        if (origHttpData)
            $_yknot.httpData = b ? origHttpData : detect;
    };

    $_yknot.ynot.replaceWithNoSelector = function (id, nodeType, search, replace) {
        // var regEx = new
        // RegExp("\b"+search.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&")+"\b",
        // 'gim');
        $_yknot(nodeType).each(
                function (index, text) {
                    // alert('regEx:'+regEx+' text:'+$_yknot(text).text())
                    text = Base64.encode($_yknot.trim($_yknot(text).html()
                            .replace(/(\n\r|\s+)/gm, '')));
                    if (text == search) {
                        // alert('matched:'+$_yknot(text).html())
                        $_yknot(this).after(
                                "<span id=\"ynot_" + id
                                + "\" class=\"ynot_modified\">"
                                + replace + "</span>");
                        $_yknot(this).remove();
                    }
                });
    }

    var logCount = 0;
    function log() {
        if (!$_yknot.ynot.debug || !window.console || !window.console.log)
            return;
        !logCount++ && log('Plugin Version: ' + version);
        window.console.log('[ynot] ' + [].join.call(arguments, ''));
    }

    var parseJSON = $_yknot.parseJSON || function (s) {
        return window['eval']('(' + s + ')');
    };

    function httpData(xhr, type, s) {
        var ct = xhr.getResponseHeader('content-type') || '', xml = type === 'xml'
                || !type && ct.indexOf('xml') >= 0, data = xml ? xhr.responseXML
                : xhr.responseText;

        if (xml && data.documentElement.nodeName === 'parsererror') {
            $_yknot.error && $_yknot.error('parsererror');
        }
        if (s && s.dataFilter) {
            data = s.dataFilter(data, type);
        }
        if (typeof data === 'string') {
            if (type === 'json' || !type && ct.indexOf('json') >= 0) {
                data = parseJSON(data);
            } else if (type === "script" || !type
                    && ct.indexOf("javascript") >= 0) {
                $_yknot.globalEval(data);
            }
        }
        return data;
    }

    function getResponse(xhr, type, s) {
        if (origHttpData)
            return origHttpData(xhr, type, s);
        return xhr.responseXML || xhr.responseText;
    }

    function detect(xhr, type, s) {
        var ct = xhr.getResponseHeader('content-type');
        if ($_yknot.ynot.debug) {
            log('[AJAX response] content-type: ', ct, ';  status: ',
                    xhr.status, ' ', xhr.statusText, ';  has responseXML: ',
                    xhr.responseXML != null);
            log('type arg: ' + type);
            // log('responseXML: ' + xhr.responseXML); // IE9 doesn't like
            // xhr.toString()
        }
        var data = getResponse(xhr, type, s);
        if (data && data.documentElement
                && data.documentElement.nodeName != 'parsererror') {
            $_yknot.ynot(data);
        } else if (typeof data == 'string') {
            // issue #4 (don't try to parse plain text or html responses
            if (/ynot/.test(data))
                $_yknot.ynot(data);
        } else {
            log('jQuery core httpData returned: ' + data);
            log('httpData: response is not XML (or not "valid" XML)');
        }
        return data;
    }

    // 1.5+ hook
    $_yknot.ajaxPrefilter
            && $_yknot.ajaxPrefilter(function (options, originalOptions, jqXHR) {
                jqXHR.success(function (data, status, jqXHR) {
                    if ($_yknot.ynot.autodetect)
                        detect(jqXHR, options.dataType, options);
                });
            });

    // < 1.5 hook
    var origHttpData = $_yknot.httpData;
    if ($_yknot.httpData)
        $_yknot.httpData = detect; // replace jQuery's httpData method

    // custom data parsers
    var parsers = {
        'json': jsonParser
    }, rawData, rawDataIndic;

    $_yknot.ynot.registerParser = function (type, fn) {
        parsers[type] = fn;
    };

    function parseRawData(type, data) {
        var d = data, parser = parsers[type];
        if ($_yknot.isFunction(parser))
            return parser(data);
        else
            throw 'No parser registered for rawData of type "' + type + '"';
    }

    function jsonParser(json) {
        return parseJSON(json);
    }

    function processDoc(xml) {
        var status = true, ex;
        try {
            if (typeof xml == 'string')
                // log(xml+"--xml doc to process");
                xml = convert(xml);
            if (!(xml && xml.documentElement)) {
                log('$_yknot.ynot invoked without valid document; nothing to process');
                return false;
            }

            var root = xml.documentElement.tagName;
            log('XML document root: ', root);

            var ynotDoc = $_yknot('ynot', xml)[0];

            if (!ynotDoc) {
                log('document does not contain <ynot> element; nothing to process');
                return false;
            }

            $_yknot.event.trigger('ynot-begin-notify', [ynotDoc]);
            status = go(ynotDoc);
        } catch (e) {
            status = ex = e;
        }
        rawDataIndic
                && $_yknot.event.trigger('ynot-rawdata-notify', [rawData]);
        $_yknot.event.trigger('ynot-complete-notify', [xml, !!status,
            status === true ? null : status]);
        if (ex)
            throw ex;
    }

    // convert string to xml document
    function convert(s) {
        var doc;
        log('attempting string to document conversion');
        try {
            if (window.DOMParser) {
                log('dom parser');
                var parser = new DOMParser();
                doc = parser.parseFromString(s, 'text/xml');
                log(doc + "--doc var");
            } else {
                log('yknot xml parser');
                doc = $_yknot("<xml>")[0];
                doc.async = 'false';
                doc.loadXML(s);
            }
        } catch (e) {
            if (window.console && window.console.error)
                window.console
                        .error('[ynot] ERROR parsing XML string for conversion: '
                                + e);
            throw e;
        }
        log(doc.documentElement + "-doc parsererror");
        var ok = doc && doc.documentElement
                && doc.documentElement.tagName != 'parsererror';
        log('conversion ', ok ? 'successful!' : 'FAILED');
        return doc;
    }

    function go(xml) {
        try {
            var t = new Date().getTime();
            // process the document
            process(xml.childNodes);
            $_yknot.ynot.lastTime = (new Date().getTime()) - t;
            log('time to process response: ' + $_yknot.ynot.lastTime + 'ms');
        } catch (e) {
            if (window.console && window.console.error)
                window.console.error('[ynot] ERROR processing document: ' + e);
            throw e;
        }
        return true;
    }

    // process the ynot commands
    function process(commands) {
        rawData = {};
        rawDataIndic = false;
        var trimHash = {
            wrap: 1
        };
        var doPostProcess = 0;
        var a, n, v, i, j, js, els, raw, type, q, jq, cdataWrap;

        for (i = 0; i < commands.length; i++) {
            if (commands[i].nodeType != 1)
                continue; // commands are elements
            var cmdNode = commands[i], cmd = cmdNode.tagName;
            if (cmd == 'eval') {
                js = (cmdNode.firstChild ? cmdNode.firstChild.nodeValue : null);
                log('invoking "eval" command: ', js);
                if (js)
                    $_yknot.globalEval(js);
                continue;
            }
            if (cmd == 'rawData') {
                raw = (cmdNode.firstChild ? cmdNode.firstChild.nodeValue : null);
                type = cmdNode.getAttribute('type');
                log('rawData (' + type + '): ', raw);

                var namespace = cmdNode.getAttribute('namespace') || 'none';

                !rawData[namespace] && (rawData[namespace] = []);

                rawData[namespace].push({
                    data: parseRawData(type, raw),
                    type: type,
                    name: cmdNode.getAttribute('name') || null,
                    raw: raw
                });
                !rawDataIndic && (rawDataIndic = true);
                continue;
            }
            q = cmdNode.getAttribute('select');
            jq = $_yknot(q);
            if (!jq[0]) {
                log('No matching targets for selector: ', q);
                continue;
            }
            cdataWrap = cmdNode.getAttribute('cdataWrap')
                    || $_yknot.ynot.defaults.cdataWrap;

            a = [];
            if (cmdNode.childNodes.length > 0) {
                doPostProcess = 1;
                for (j = 0, els = []; j < cmdNode.childNodes.length; j++)
                    els[j] = createNode(cmdNode.childNodes[j], cdataWrap);
                a.push(trimHash[cmd] ? cleanse(els) : els);
            }

            // remain backward compat with pre 2.0.9 versions
            n = cmdNode.getAttribute('name');
            v = cmdNode.getAttribute('value');
            if (n !== null)
                a.push(n);
            if (v !== null)
                a.push(v);

            // @since: 2.0.9: support arg1, arg2, arg3...
            for (var j = 1; true; j++) {
                v = cmdNode.getAttribute('arg' + j);
                if (v === null)
                    break;
                // support numeric primitives
                if (v.length) {
                    var n = Number(v);
                    if (v == n)
                        v = n;
                }
                a.push(v);
            }

            $_yknot.ynot.debug && logCommand(q, cmd, a, els);
            jq[cmd].apply(jq, a);

            // jq['decodeURI'].apply(jq,a);
        }

        // apply dynamic fixes
        doPostProcess && postProcess();
    }

    function logCommand(q, cmd, a, els) {

        var args = '...';
        if (!els) {
            args = '';
            for (var k = 0, val = a[0]; k < a.length, val = a[k]; k++) {
                k > 0 && (args += ',');
                typeof val == 'string' ? (args += ("'" + val + "'"))
                        : (args += val);
            }
        }
        log("invoking command: $_yknot('", q, "').", cmd, '(' + args + ')');
    }

    function postProcess() {

        // post processing fixes go here; currently there is only one:
        // fix1: opera, IE6, Safari/Win don't maintain selected options in all
        // cases (thanks to Karel Fuc�k for this!)
        $_yknot('select:ynotTag').each(function () {
            var sel = this;
            $_yknot('option:ynotTag', this).each(function () {
                this.setAttribute('selected', 'selected');
                this.ynotTag = null;
                if (sel.type == 'select-one') {
                    var idx = $_yknot('option', sel).index(this);
                    sel.selectedIndex = idx;
                }
            });
            this.ynotTag = null;
        });

    }

    function cleanse(els) {
        for (var i = 0, a = []; i < els.length; i++)
            if (els[i].nodeType == 1)
                a.push(els[i]);

        return a;
    }

    function createNode(node, cdataWrap) {
        var type = node.nodeType;
        if (type == 1)
            return createElement(node, cdataWrap);
        if (type == 3)
            return fixTextNode(node.nodeValue);
        if (type == 4)
            return handleCDATA(node.nodeValue, cdataWrap);
        return null;
    }

    function handleCDATA(s, cdataWrap) {
        var el = document.createElement(cdataWrap);
        var $el = $_yknot(el)[cdataWrap == 'script' ? 'text' : 'html'](s);
        var $ch = $el.children();

        // remove wrapper node if possible
        if ($ch.size() == 1)
            return $ch[0];
        return el;
    }

    function fixTextNode(s) {
        if (ie_version > 0)
            s = s.replace(/\n/g, '\r').replace(/\s+/g, ' ');
        return document.createTextNode(s);
    }

    function createElement(node, cdataWrap) {
        var e, tag = node.tagName.toLowerCase();
        // some elements in IE need to be created with attrs inline
        if (ie_version > 0 && ie_version < 9) {
            var type = node.getAttribute('type');
            if (tag == 'table' || type == 'radio' || type == 'checkbox'
                    || tag == 'button'
                    || (tag == 'select' && node.getAttribute('multiple'))) {
                e = document.createElement('<' + tag + ' '
                        + copyAttrs(null, node, true) + '>');
            }
        }
        if (!e) {
            e = document.createElement(tag);
            copyAttrs(e, node);
        }

        // IE fix; colspan must be explicitly set
        if (ie_version > 0 && tag == 'td') {
            var colspan = node.getAttribute('colspan');
            if (colspan)
                e.colSpan = parseInt(colspan);
        }

        // IE fix; script tag not allowed to have children
        if (ie_version > 0 && !e.canHaveChildren) {
            if (node.childNodes.length > 0)
                e.text = node.text;
        } else {
            for (var i = 0, max = node.childNodes.length; i < max; i++) {
                var child = createNode(node.childNodes[i], cdataWrap);
                if (child)
                    e.appendChild(child);
            }
        }
        if (!is_firefox) {
            if (tag == 'select'
                    || (tag == 'option' && node.getAttribute('selected')))
                e.ynotTag = 1;
        }
        return e;
    }

    function copyAttrs(dest, src, inline) {
        for (var i = 0, attr = ''; i < src.attributes.length; i++) {
            var a = src.attributes[i], n = $_yknot.trim(a.name), v = $_yknot
                    .trim(a.value);
            if (inline)
                attr += (n + '="' + v + '" ');
            else if (n == 'style') { // IE workaround
                dest.style.cssText = v;
                dest.setAttribute(n, v);
            } else
                $_yknot.attr(dest, n, v);
        }
        return attr;
    }

    function decodeHTMLEntities(str) {
        var element = document.createElement('div');

        if (str && typeof str === 'string') {
            // strip script/html tags
            str = str.replace(/<script[^>]*>([\S\s]*?)<\/script>/gmi, '');
            str = str.replace(/<\/?\w(?:[^"'>]|"[^"]*"|'[^']*')*>/gmi, '');
            element.innerHTML = str;
            str = element.textContent;
            element.textContent = '';
        }
        return str;
    }

    var Base64 = {
        // private property
        _keyStr: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=",
        // public method for encoding
        encode: function (input) {
            var output = "";
            var chr1, chr2, chr3, enc1, enc2, enc3, enc4;
            var i = 0;

            input = Base64._utf8_encode(input);

            while (i < input.length) {

                chr1 = input.charCodeAt(i++);
                chr2 = input.charCodeAt(i++);
                chr3 = input.charCodeAt(i++);

                enc1 = chr1 >> 2;
                enc2 = ((chr1 & 3) << 4) | (chr2 >> 4);
                enc3 = ((chr2 & 15) << 2) | (chr3 >> 6);
                enc4 = chr3 & 63;

                if (isNaN(chr2)) {
                    enc3 = enc4 = 64;
                } else if (isNaN(chr3)) {
                    enc4 = 64;
                }

                output = output + this._keyStr.charAt(enc1)
                        + this._keyStr.charAt(enc2) + this._keyStr.charAt(enc3)
                        + this._keyStr.charAt(enc4);

            }

            return output;
        },
        // public method for decoding
        decode: function (input) {
            var output = "";
            var chr1, chr2, chr3;
            var enc1, enc2, enc3, enc4;
            var i = 0;

            input = input.replace(/[^A-Za-z0-9\+\/\=]/g, "");

            while (i < input.length) {

                enc1 = this._keyStr.indexOf(input.charAt(i++));
                enc2 = this._keyStr.indexOf(input.charAt(i++));
                enc3 = this._keyStr.indexOf(input.charAt(i++));
                enc4 = this._keyStr.indexOf(input.charAt(i++));

                chr1 = (enc1 << 2) | (enc2 >> 4);
                chr2 = ((enc2 & 15) << 4) | (enc3 >> 2);
                chr3 = ((enc3 & 3) << 6) | enc4;

                output = output + String.fromCharCode(chr1);

                if (enc3 != 64) {
                    output = output + String.fromCharCode(chr2);
                }
                if (enc4 != 64) {
                    output = output + String.fromCharCode(chr3);
                }

            }

            output = Base64._utf8_decode(output);

            return output;

        },
        // private method for UTF-8 encoding
        _utf8_encode: function (string) {
            string = string.replace(/\r\n/g, "\n");
            var utftext = "";

            for (var n = 0; n < string.length; n++) {

                var c = string.charCodeAt(n);

                if (c < 128) {
                    utftext += String.fromCharCode(c);
                } else if ((c > 127) && (c < 2048)) {
                    utftext += String.fromCharCode((c >> 6) | 192);
                    utftext += String.fromCharCode((c & 63) | 128);
                } else {
                    utftext += String.fromCharCode((c >> 12) | 224);
                    utftext += String.fromCharCode(((c >> 6) & 63) | 128);
                    utftext += String.fromCharCode((c & 63) | 128);
                }

            }

            return utftext;
        },
        // private method for UTF-8 decoding
        _utf8_decode: function (utftext) {
            var string = "";
            var i = 0;
            var c = c1 = c2 = 0;

            while (i < utftext.length) {

                c = utftext.charCodeAt(i);

                if (c < 128) {
                    string += String.fromCharCode(c);
                    i++;
                } else if ((c > 191) && (c < 224)) {
                    c2 = utftext.charCodeAt(i + 1);
                    string += String.fromCharCode(((c & 31) << 6) | (c2 & 63));
                    i += 2;
                } else {
                    c2 = utftext.charCodeAt(i + 1);
                    c3 = utftext.charCodeAt(i + 2);
                    string += String.fromCharCode(((c & 15) << 12)
                            | ((c2 & 63) << 6) | (c3 & 63));
                    i += 3;
                }
            }
            return string;
        }

    }

    function getMSIe() {
        var rv = -1;
        if (navigator.appName == 'Microsoft Internet Explorer') {
            var ua = navigator.userAgent;
            var re = new RegExp("MSIE ([0-9]{1,}[\.0-9]{0,})");
            if (re.exec(ua) != null)
                rv = parseFloat(RegExp.$1);
        }
        return rv;
    }
})($_yknot);

$_yknot(document).ready(function () {
    if (typeof yknotCamp !== 'undefined') {
        // alert(yknotCamp);
        $_yknot.ynot(yknotCamp);
    }
});

$_yknot(function () {
    ts = new Date(2012, 0, 1);

    if ((new Date()) > ts) {
        // The new year is here! Count towards something else.
        // Notice the *1000 at the end - time must be in milliseconds
        ts = (new Date()).getTime() + 5 * 60 * 1000;
        newYear = false;
    }
    if (typeof $_yknot('#countdown').val() != "undefined") {
        $_yknot('#countdown').countdown({
            timestamp: ts,
            callback: function (days, hours, minutes, seconds) {
            }
        });

    }

    if (typeof $_yknot('#swiper-container').val() != "undefined") {
        var swiper = $_yknot('#swiper-container');
        var mySwiper = new Swiper('#swiper-container', {
            pagination: '.pagination',
            loop: true,
            grabCursor: true,
            paginationClickable: true
        })
        $_yknot('.arrow-left').on('click', function (e) {
            alert('yep');
            e.preventDefault();
            mySwiper.swipePrev();
        })
        $_yknot('.arrow-right').on('click', function (e) {
            e.preventDefault();
            mySwiper.swipeNext();
        })

    }

});//nx